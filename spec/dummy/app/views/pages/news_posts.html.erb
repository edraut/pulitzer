<% @pulitzer_posts.each do |post| %>
  <h1><%= post.title %></h1>
  <div class="image_with_caption">
    <%= image_tag post.content_element('Background').image_url %>
  </div>
  <p><%= post.content_element('Main Body').body %></p>
<% end %>
