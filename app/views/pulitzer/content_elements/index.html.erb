<h1>Content elements for <%= @post.title %></h1>

<% @content_elements.each do |content_element| %>
  <%= render partial: 'show_wrapper', locals: {content_element: content_element} %>
<% end %>

<%= link_back_to_posts(@post.post_type) %>
