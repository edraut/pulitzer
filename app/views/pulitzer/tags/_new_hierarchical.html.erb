<% container = @tag.parent_id? ? "#new_tag_#{@tag.parent_id}" : '#new_tag' %>
<% target = @tag.parent_id? ? "##{dom_id(@tag.parent)}_children" : "#tags_container" %>
<%= render partial: 'new', locals: { target: target, container: container } %>
